## 自定义指令
```text
概念:为了对底层dom进行操作
语法:Vue.directive('name',{
  inserted(){},
  update(){}
})
简写:Vue.directive('name',()=>{})  在insert和update时触发相同行为
可接受参数:
el:指令所绑定的元素，可以直接操作dom元素
binding:可接受传递进来的值
生命周期:
bind  初始化时
inserted  元素插入到父节点时
update  数据发生改变时
componentUpdated  数据发生改变之后
unbind  指令与元素解绑时


```